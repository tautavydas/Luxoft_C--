#include "object.h"


void myfunc() {
  auto obj = makeObject(1.4);
  // Object<double> obj(10);
  // Object<double>::ContainerType vec = obj.get();
  // std::vector<Object<double>::ContainerType> vec2;
  // std::copy(vec.begin(), vec.end(), std::back_inserter(vec2));
  // obj.foo();
}